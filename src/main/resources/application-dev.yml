spring:
  docker:
    compose:
      enabled: true
      file: compose.yaml
  datasource:
    url: jdbc:postgresql://localhost:5432/markman
    username: markman
    password: markman
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.markman: DEBUG
    org.springframework.modulith: DEBUG
